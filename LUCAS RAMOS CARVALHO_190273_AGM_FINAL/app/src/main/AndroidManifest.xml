<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.agenda">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Agenda">
        <activity android:name=".ui.activities.CadastroActivity"/>

        <activity android:name=".ui.activities.CadastroListaActivity">
            //activity: lança as atividades.
            <intent-filter>
                //intent=filter: inicia as atividades e permite que o programa seja instalado.
                <action android:name="android.intent.action.MAIN" />
                //action: adiciona uma ação ao intent-filter.
                <category android:name="android.intent.category.LAUNCHER" />
                //category: adiciona um nome de categoria ao intent-filter.
            </intent-filter>
        </activity>
    </application>

</manifest>